# ===========================================
# YourFellow Performance - Environment Variables
# ===========================================
# Copy this file to .env.local and fill in your values

# Supabase Configuration
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Anthropic API (Claude)
# Get this from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# Optional: OpenAI for image generation (DALL-E)
# Get this from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-key-here

# Optional: Replicate for alternative image generation
# Get this from: https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=r8_your-replicate-token-here

# WebSearch (Tavily) - For live internet search in chat
# Get this from: https://tavily.com/ (free tier: 1000 searches/month)
TAVILY_API_KEY=tvly-your-key-here

# ClickUp Integration - For task management per client
# Get this from: https://app.clickup.com/settings/apps (Personal API Token)
CLICKUP_API_KEY=pk_your-clickup-api-key-here

# App Configuration
# For local development:
NEXT_PUBLIC_APP_URL=http://localhost:3000
# For production (hq.yourfellow.nl):
# NEXT_PUBLIC_APP_URL=https://hq.yourfellow.nl
NEXT_PUBLIC_APP_NAME="YourFellow HQ"

# ===========================================
# GA4 Anomaly Monitoring Service
# ===========================================

# Config source: 'database' (default, uses client settings from Supabase) or 'file'
CONFIG_SOURCE=database

# Supabase service role key (required for database config)
# Get this from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
# WARNING: This key has full database access - keep it secret!
SUPABASE_SERVICE_KEY=your-service-role-key-here

# File config path (only used when CONFIG_SOURCE=file)
CONFIG_PATH=./config/monitoring.json

# Google Analytics Data API credentials
# Create a service account at: https://console.cloud.google.com/iam-admin/serviceaccounts
# Enable GA4 Data API: https://console.cloud.google.com/apis/library/analyticsdata.googleapis.com
# Add the service account email as viewer to your GA4 properties
# Download the JSON key and paste the ENTIRE content here (as a single line)
GA4_CREDENTIALS={"type":"service_account","project_id":"your-project",...}

# ClickUp API token for alert tasks
# Get this from: https://app.clickup.com/settings/apps (Personal API Token)
# Note: Same as CLICKUP_API_KEY above, but used by monitoring service
CLICKUP_TOKEN=pk_your-clickup-api-key-here

# Path to store alert fingerprints (for idempotency)
STORE_PATH=./data/fingerprints.json

# Set to 'true' for dry run mode (no ClickUp tasks created)
DRY_RUN=false

# Log level: debug, info, warn, error
LOG_LEVEL=info

# ===========================================
# SEO Content Advisory Module
# ===========================================

# Google Search Console API credentials
# Uses the same service account as GA4 (or a separate one)
# The service account needs to be added as a user to your Search Console property
# Grant "Full" access at: https://search.google.com/search-console/users
# If not set, falls back to GA4_CREDENTIALS
SEARCH_CONSOLE_CREDENTIALS={"type":"service_account","project_id":"your-project",...}

# Default Search Console property URL
# Format: https://example.com/ (URL prefix) or sc-domain:example.com (domain property)
# Can be overridden via CLI: --site-url=https://example.com
# SEARCH_CONSOLE_SITE_URL=https://yoursite.com/

# ===========================================
# Meta Ads Integration (Facebook/Instagram)
# ===========================================
# Note: Most Meta credentials are stored per-client in the database.
# The following are only needed if you want a default/fallback configuration.

# ===========================================
# Email Service (Resend)
# ===========================================
# Used for workflow email nodes
# Get this from: https://resend.com/api-keys
RESEND_API_KEY=re_your-resend-api-key-here

# Optional: Custom from address (must be verified in Resend)
# EMAIL_FROM=workflows@yourdomain.com

# ===========================================
# Meta Ads Integration (Facebook/Instagram)
# ===========================================

# Meta App ID (from https://developers.facebook.com/apps/)
# META_APP_ID=524305247418195

# Meta App Secret (from App Settings > Basic)
# WARNING: Keep this secret! Never commit to version control.
# META_APP_SECRET=your-app-secret-here

# Default access token for testing (optional)
# Generate at: https://developers.facebook.com/tools/explorer/
# For production, tokens are stored per-client in the database
# META_ACCESS_TOKEN=EAAxxxxxx...
